import { Bucket } from '../types';

export const SLIDESHOW_INTERVAL = 5000;
export const DEFAULT_BUCKET = Bucket.both;
export const ICON_SIZE = 20;
export const CLOUDFRONT_DIST_DOMAIN = 'https://d2jsbe4kz9t2p7.cloudfront.net';
